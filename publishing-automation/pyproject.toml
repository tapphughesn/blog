[project]
name = "publishing-automation"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "google-auth",
    "google-auth-oauthlib",
    "google-api-python-client",
    "mammoth",
    "beautifulsoup4",
    "Pillow",
]
